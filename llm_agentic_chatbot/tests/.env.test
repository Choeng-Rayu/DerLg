# =============================================================================
# Test Environment Variables for DerLg.com AI Agent
# =============================================================================
# These are dummy/test values for running the test suite.
# NEVER use these values in production.

# LLM Backend Configuration
MODEL_BACKEND=anthropic
ANTHROPIC_API_KEY=sk-ant-test-key-for-testing-only-1234567890

# Ollama (not used when MODEL_BACKEND=anthropic, included for completeness)
OLLAMA_BASE_URL=http://localhost:11434

# Backend API Configuration
BACKEND_URL=http://localhost:3001
AI_SERVICE_KEY=test-service-key-for-unit-tests-only-minimum-32-chars

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Server Configuration
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=debug

# Error Tracking (disabled in tests)
SENTRY_DSN=
